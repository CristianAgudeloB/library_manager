<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= comic.title %></title>
    <link rel="icon" type="image/x-icon" href="/images/favicon.png">
    <link rel="stylesheet" href="/styles.css">
    <!-- Slick Carousel CSS -->
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css"
    />
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick-theme.css"
    />
</head>
<body>

        <!-- jQuery (requerido por Slick Carousel) -->
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <!-- Slick Carousel JS -->
        <script src="https://cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
        <!-- Script para mostrar/ocultar la barra de búsqueda -->
    <!-- Menú superior -->
    <header>
        <div class="header-logo">
            <a href="/">
                <img src="/images/logo.png" alt="Logo de la Biblioteca de Cómics">
            </a>
        </div>
        <div class="menu-toggle">&#9776;</div> <!-- Botón de menú hamburguesa -->
        <nav class="menu">
            <ul>
                <li><a href="/">Inicio</a></li>
                <li><a href="/marvel">Marvel</a></li>
                <li><a href="/dc">DC</a></li>
                <li><a href="/indie">Indie</a></li>
                <li><a href="/novedades">Novedades</a></li>
                <li><a href="/el-corps">El Corps</a></li>
            </ul>
            <!-- Barra de búsqueda -->
            <div class="search-container">
                <div class="search-icon" onclick="toggleSearch()">
                    <img src="/images/lupa.png" alt="Buscar">
                </div>
                <form action="/buscar" method="GET" class="search-form">
                    <input type="text" name="q" placeholder="Buscar por título o volumen..." required>
                    <button type="submit">Buscar</button>
                </form>
            </div>
        </nav>
    </header>

    <!-- Contenido principal -->
    <main class="comic-detail">
        <!-- Sección izquierda: Imagen y título -->
        <div class="comic-left">
            <img src="<%= comic.coverUrl %>" alt="<%= comic.title %>">
            <h2><%= comic.title %></h2>
            <a href="/series/<%= comic.volume %>" class="btn-series">Ver Serie</a>
        </div>

        <!-- Sección derecha: Información adicional -->
        <div class="comic-right">
            <!-- Caja de descripción y equipo de tradumaqueta -->
            <div class="info-box">
                <h3>Descripción</h3>
                <p><%= comic.descripcion %></p>
                <div class="team-info">
                    <p><strong>Traductor:</strong> <%= comic.traductor %></p>
                    <p><strong>Maqueta:</strong> <%= comic.maqueta %></p>
                    <p><strong>Corrector:</strong> <%= comic.corrector %></p>
                </div>
            </div>

<!-- Caja de enlaces de descarga y leer online -->
<div class="info-box">
    <h3>Enlaces de descarga</h3>
    <ul>
        <% comic.downloadUrls.forEach(url => { %>
            <li>
                <a href="<%= url %>" target="_blank">
                    Descargar (<%= url.includes('mega') ? 'Mega' : url.includes('mediafire') ? 'Mediafire' : 'Enlace' %>)
                </a>
            </li>
        <% }) %>
    </ul>
    <% if (comic.pages && comic.pages.length > 0) { %>
        <a href="/comic/<%= comic._id %>/read" class="btn-read-online">Leer Online</a>
    <% } %>
</div>

    </main>
<!-- Sección de Cómics Relacionados -->
<section class="related-comics">
    <h2>Cómics relacionados</h2>
    <div class="comics-slider">
      <% if (relacionados.length > 0) { %>
        <% relacionados.forEach(comic => { %>
          <div class="comic-slide">
            <div class="comic-card">
              <a href="/comic/<%= comic._id %>">
                <img src="<%= comic.coverUrl %>" alt="<%= comic.title %>">
                <h3><%= comic.title %></h3>
              </a>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p>No hay cómics relacionados.</p>
      <% } %>
    </div>
  </section>


    <footer>
        <p>Inifinity Comics 2025 - Sin ánimo de lucro. Todos los derechos reservados a sus respectivos creadores y propietarios.</p>
        <p>Creado por Mastercraft Web Developing</p>
    </footer>
    <script>
        
        function toggleSearch() {
            const searchForm = document.querySelector('.search-form');
            searchForm.classList.toggle('active');
        }
        document.querySelector('.menu-toggle').addEventListener('click', function () {
            const menu = document.querySelector('.menu ul');
            menu.classList.toggle('active');
        });
    </script>

<script>
    $(document).ready(function () {
      $('.related-comics .comics-slider').slick({
        infinite: true,
    slidesToShow: 5, // Pantallas grandes
    slidesToScroll: 1,
    arrows: true,
    dots: false,
    prevArrow: '<button type="button" class="slick-prev">←</button>',
    nextArrow: '<button type="button" class="slick-next">→</button>',
        responsive: [
      {
        breakpoint: 1280, // Laptops grandes
        settings: {
          slidesToShow: 4,
          slidesToScroll: 1,
        },
      },
      {
        breakpoint: 1024, // Tablets en landscape
        settings: {
          slidesToShow: 3,
          slidesToScroll: 1,
          centerMode: true,
          centerPadding: "20px", // Ajusta el espacio a los lados
        },
      },
      {
        breakpoint: 900, // Entre 900px y 1024px
        settings: {
          slidesToShow: 3,
          slidesToScroll: 1,
          centerMode: false,
        },
      },
      {
        breakpoint: 820, // Por ejemplo, en 820px de ancho se mostrará 2 slides con ligero centrado
        settings: {
          slidesToShow: 3,
          slidesToScroll: 1,
          centerMode: true,
          centerPadding: "10px",
        },
      },
      {
        breakpoint: 768, // En 768px (tablets en portrait o algunos teléfonos grandes)
        settings: {
          slidesToShow: 3,
          slidesToScroll: 1,
          centerMode: false,
        },
      },
      {
        breakpoint: 480, // Para teléfonos pequeños: se muestran 2 slides
        settings: {
          slidesToShow: 2,
          slidesToScroll: 1,
          centerMode: false,
        },
      },
      {
        breakpoint: 360, // En dispositivos extremadamente estrechos
        settings: {
          slidesToShow: 1,
          slidesToScroll: 1,
        },
      },
    ],
      });
    });
  </script>
  
</body>
</html>
</body>
</html>